1.git是一种对文件修改进行管理的工具。
2.git分为工作区，缓存区。在工作区进行文件的添加删除修改，缓存区作为保存你对文件操作的内容。
3.每一次的修改一定要上传到缓存区，然后在确定上交（commit）。
4.创建一个属于自己的个人工作分支，以避免对主分支 master 造成太多的干扰，也方便与他人交流协作。
5.当进行高风险的工作时，创建一个试验性的分支，扔掉一个烂摊子总比收拾一个烂摊子好得多。
6.合并别人的工作的时候，最好是创建一个临时的分支。